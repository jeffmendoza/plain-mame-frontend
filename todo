
conf/ini mame dirs - real run
    make ini, read mame_bin from ini, etc
    read romdir from mame ini
    logic to create mame.xml

autotools - make install?
which way to go?
      autotools - make
      distutils - stdeb - try for now
      standard deb stuff?

rpm/deb packaging

next ver:

curses game info and prompt
pause after mame run, hit key
esc to go back, not just "back"
left/right arrows too
logging
GUI
